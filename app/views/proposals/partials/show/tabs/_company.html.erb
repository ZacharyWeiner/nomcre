<style type="text/css">
  hr { margin-bottom: -1px; margin-top: -2px}
</style>
<% if proposal.is_editable %>
  <%= render :partial => 'proposals/partials/show/tabs/company/editable', :locals => {proposal: proposal} %>
<% else %>
  <%= render :partial => 'proposals/partials/show/tabs/company/display_only', :locals => {proposal: proposal} %>
<% end %>
