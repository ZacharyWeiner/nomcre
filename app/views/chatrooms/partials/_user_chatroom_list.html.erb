

<div class="people-list" id="people-list">
  <ul class="list" style="list-style: none;">
    <% current_user.chatrooms.uniq.each do |room| %>
      <li class="clearfix">
        <%= image_tag room.messages.last.user.profile_image unless room.messages.last.nil? %>
        <div class="about">
          <div class="name"><%= link_to room.proposal.title, chatroom_path(room), style: 'color:white; text-decoration: none;' %></div>
          <div class="status">
          </div>
        </div>
      </li>
    <% end %>
  </ul>
</div>
