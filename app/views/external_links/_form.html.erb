<%= form_with(model: external_link, local: true) do |form| %>
  <% if external_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(external_link.errors.count, "error") %> prohibited this external_link from being saved:</h2>

      <ul>
      <% external_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :external_link_title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :external_link_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
